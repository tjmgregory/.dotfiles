You must never, ever do compound commands with `cd` or `git`.
Always separate calls to `cd` or `git` as isolated, individual commands.
You will typically work for a long time in a given directory, so simply cd into it when needed.
